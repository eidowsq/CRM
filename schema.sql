CREATE DATABASE IF NOT EXISTS crm DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE crm;

-- Beego ORM will create and evolve the tables at startup.
-- This file intentionally only prepares the database and encoding.
